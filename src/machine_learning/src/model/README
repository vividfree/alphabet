对 supervised_model 类设计考虑

在取模型参数这块，至少有下面2种方案：
(1) 方案1（也是当前采用的方案）是把模型参数通过构造函数送入，这样各个接口可以自由取模型参数；
(2) 方案2是在构造函数中不传入模型参数，而是在各个接口中传入模型参数。

方案1的优点：使得 SupervisedModel 类能够被上层做抽象使用，这样设计才能做继承。
考虑到方案1的优点，所以就选用方案1实现。
